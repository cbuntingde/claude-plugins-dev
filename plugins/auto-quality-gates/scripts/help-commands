#!/bin/bash
# Help Commands Entry Point
# This script is the entry point for the help-commands command

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the help-commands.js script
node "$SCRIPT_DIR/help-commands.js" "$@"